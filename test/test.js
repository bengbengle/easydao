// const _createNewAmountCheckpoint = (member, block_number, tokenIds) => {

//     let checkpoints = new Map();
//     let numCheckpoints = new Map();

//     let nCheckpoints = numCheckpoints.get(member) || 0;
    
//     let checkpoint = new Map().set(block_number, tokenIds);

//     checkpoints.get(member).set(nCheckpoints, checkpoint);
            
//     numCheckpoints.se(member)  += 1
// }

// // _createNewAmountCheckpoint(member, token, newAmount);